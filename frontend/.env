# 前端环境变量示例（Vite，仅 VITE_ 前缀的变量会暴露给 import.meta.env）
# 默认不设置 VITE_APP_URL：请求走相对路径 /api/**，
#   - dev 下由 Vite 代理转发到后端 http://localhost:8000
#   - 生产下前后端同源（都由后端 8000 提供）直连即可
# 如需显式指定后端地址，取消下面注释：
# VITE_APP_URL=http://localhost:8000
